{
  "name": "color-rush-app",
  "version": "1.0.0",
  "private": true,
  "description": "Wrapper nativo (Capacitor) do Color Rush para iOS/Android — carrega colorrush.com.br remotamente e adiciona push notifications + haptics + avaliação in-app.",
  "scripts": {
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/app": "^8.0.0",
    "@capacitor/haptics": "^8.0.0",
    "@capacitor-community/in-app-review": "^8.0.0",
    "@capacitor-firebase/authentication": "^8.0.0",
    "@capacitor-firebase/messaging": "^8.0.0",
    "@revenuecat/purchases-capacitor": "^13.0.0",
    "firebase": "^12.6.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
