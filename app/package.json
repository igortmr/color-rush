{
  "name": "color-rush-app",
  "version": "1.0.0",
  "private": true,
  "description": "Wrapper nativo (Capacitor) do Color Rush para iOS/Android — carrega colorrush.com.br remotamente e adiciona push notifications + haptics.",
  "scripts": {
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/push-notifications": "^8.0.0",
    "@capacitor/haptics": "^8.0.0",
    "@capacitor-firebase/authentication": "^8.0.0",
    "firebase": "^10.12.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
